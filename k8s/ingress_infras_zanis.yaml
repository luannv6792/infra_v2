apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: infra-ingress
  namespace: app
spec:
  ingressClassName: nginx
  rules:
    - host: infras.zanis.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: infraweb
                port:
                  number: 80
